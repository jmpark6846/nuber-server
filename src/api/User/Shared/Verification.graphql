type Verification {
  id: Int!
  target: String! # phone, email
  payload: String! # phone number 혹은 email
  key: String! # 인증번호
  createdAt: String!
  updatedAt: String
}

type Query {
  verification: Verification
}
